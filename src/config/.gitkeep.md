# Reserved for future runtime configuration

As more third-party integrations are added (analytics, payments, feature
flags), centralised runtime config (distinct from the static content in
src/data/) belongs here — e.g. a single file exporting which features are
enabled based on environment variables, rather than scattering
`import.meta.env` checks across components.
