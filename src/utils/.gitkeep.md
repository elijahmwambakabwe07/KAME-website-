# Reserved for future shared utility functions

As the codebase grows, generic helpers that don't belong to any one
component (date formatting, string truncation, currency formatting for
ZMW, etc.) belong here rather than being duplicated across pages.
